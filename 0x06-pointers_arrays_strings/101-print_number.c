#include "main.h"

/**
 * print_number - prints an integer
 * @n: integer
 * Return: result
 */
void print_number(int n)
{
	int i, j;
	int a = 0;
	int m;

	if (n == 0)
		_putchar('0');
	if (n < 0)
	{
		_putchar ('-');
		n *= -1;
		n++;
	}
	for (i = 0; i < a; i++)
		m *= 10;
}
