#include <stdio.h>

/**
 * main - print all single digit of base 10
 * Return: 0 Always
 */
int main(void)
{
	int i;

	i = 48;
	while (i != 58)
	{
		putchar(i);
		i++;
	}
	putchar('\n');
	return (0);
}
